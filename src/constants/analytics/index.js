import {
  ANALYTICS_EVENTS_NAME,
  formatViewEvents,
  formatCartEvents,
  formatErrorEvents
} from './eventsName';

const ANALYTICS_DELAY_LOG_EVENT = 1200;

export {
  ANALYTICS_EVENTS_NAME,
  ANALYTICS_DELAY_LOG_EVENT,
  formatViewEvents,
  formatCartEvents,
  formatErrorEvents
};
