import {
  isConfigActive,
  getValueFromConfigKey,
  INPUT_ADDRESS_TYPE,
} from './configKeyHandler';
import {CONFIG_KEY} from './types';

export {isConfigActive, getValueFromConfigKey, CONFIG_KEY, INPUT_ADDRESS_TYPE};
